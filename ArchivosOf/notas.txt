los metodos para HU8 estan sacando informacion de un smb.conf local, NO el original
    -los cambios que realiza los guarda en un archivo llamado probando.txt
        -hay que reemplazar probando.txt por smb.conf original.

IMPORTANTE:
al cambiar nombre de recurso de "Archivo" a "archivo" se presenta un error
    -Evitar poner nuevos nombres que contengan > %D0A algunos de esos elementos

-crear metodo que valide una ruta que se ingrese por el programa
-revisar los metodos para las HUS 1y2

-aplicar metodo lanzador a HU5y6 para que reemplace el archivo "probando2.txt"(hu6) por el smb.conf original

-cambiar rutas de archivo original de hu7 directamente en la variable "archSamba"